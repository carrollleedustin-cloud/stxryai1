'use client';

/**
 * HEADER COMPONENT (Legacy)
 * This component is now replaced by EtherealNav for most pages.
 * Kept for backwards compatibility with older page structures.
 */

import React from 'react';
import EtherealNav from '@/components/void/EtherealNav';

export default function Header() {
  return <EtherealNav />;
}
